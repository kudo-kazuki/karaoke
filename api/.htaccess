<IfModule mod_rewrite.c>
    RewriteEngine On

    # 既存ファイル・ディレクトリはそのまま返す
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d

    # それ以外はすべて index.php に転送
    RewriteRule ^ index.php [L]
</IfModule>
